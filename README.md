# st-tool

`st-tool` is a minimal Node.js script that prints a friendly message to verify that your runtime environment is working.

## Prerequisites
- Node.js 18 or later

## Usage
Run the script directly with Node:

```bash
node index.js
```

You should see a short greeting logged to the console.
